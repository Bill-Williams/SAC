@model IEnumerable<SAC.Domain.Models.Award>

@{
    ViewBag.Title = "Manage Awards";
}

<div class="well">
    @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-default", style = "float:right" })
    <h2>
        @ViewBag.Title
    </h2>
</div>

<div class="panel">
    <div class="panel-body">
        <table class="table table-striped table-hover tbl_pointer" id="tblAwards">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Name)
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr onclick="location.href = '@(Url.Action("Edit", new { id = item.Id }))'">
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/datatables")
    <script>
        $(document).ready(function () {
            $('#tblAwards').DataTable();
        });
    </script>
}
